# Changesets

This folder is managed by [Changesets](https://github.com/changesets/changesets).

- Run `npx changeset` after making changes that should be released.
- Commit the generated markdown file in this folder with your feature/fix PR.
- Release automation in GitHub Actions consumes these files to version and publish packages.
